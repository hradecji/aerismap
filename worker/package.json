{
  "name": "@aerismap/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "seed:local": "for f in stations.geojson.gz areas.json.gz temp-isobands.geojson.gz aqi-grid.geojson.gz meta.json; do if [ -f ../ingest/.artifacts/latest/$f ]; then wrangler kv key put --binding DATA \"latest/$f\" --path ../ingest/.artifacts/latest/$f --local || exit 1; fi; done",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aerismap/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260714.1",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
